{
  "name": "league",
  "version": "1.0.0",
  "description": "motogp league",
  "main": "index.js",
  "scripts": {
    "start": "set NODE_ENV=dev && npm-run-all --parallel dev-server dev-css",
    "dev-server": "lite-server index.html --serveStatic json",
    "dev-css": "node-sass-chokidar src/ -o src/ --watch --recursive --source-map true",
    "test": "jest --watch",
    "build": "parcel build index.html --out-dir dist",
    "prod-server": "lite-server dist/index.html",
    "prod": "set NODE_ENV=prod && parcel index.html --out-dir dist"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.motogp.net:axelclaver/league.git"
  },
  "keywords": [
    "state",
    "management",
    "es6",
    "testing"
  ],
  "author": "Axel claver",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^22.0.1",
    "babel": "^6.23.0",
    "babel-core": "^6.26.0",
    "babel-jest": "^22.0.6",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-2": "^6.24.1",
    "jest": "^22.0.6",
    "lite-server": "^2.3.0",
    "node-sass-chokidar": "0.0.3",
    "npm-run-all": "^4.1.2",
    "parcel-bundler": "^1.2.1",
    "regenerator-runtime": "^0.11.1"
  }
}
